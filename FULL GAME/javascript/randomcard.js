$(document).ready(function() {

});
function randomCard()
{
  let num=Math.floor((Math.random() * 13) + 1);  //bolo 12+1 , zmena na 13+1 pretoze nie bol Kral.
  return num;
}

// Sasha
function randomPcCard()
{
    let card=Math.floor((Math.random() * 13) + 1);  //bolo 12+1 , zmena na 13+1 pretoze nie bol Kral.
    return card;
}
